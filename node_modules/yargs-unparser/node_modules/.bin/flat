#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_flat@4.1.0@flat/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_flat@4.1.0@flat/cli.js" "$@"
  ret=$?
fi
exit $ret
